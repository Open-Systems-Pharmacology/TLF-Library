# .createPlotLegendPosition

Create legend position directly usable by `ggplot2`

## Usage

``` r
.createPlotLegendPosition(position)
```

## Arguments

- position:

  Position of legend as defined in enum `LegendPositions`

## Value

A list with fields `xPosition`, `xJustification`, `yPosition`,
`yJustification`
