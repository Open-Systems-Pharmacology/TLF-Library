# useDarkTheme

Set default Matlab theme to be used as the current default of the tlf
environment

## Usage

``` r
useDarkTheme()
```

## Examples

``` r
useDarkTheme()
addScatter(x = seq(1, 10), y = rnorm(10))
```
