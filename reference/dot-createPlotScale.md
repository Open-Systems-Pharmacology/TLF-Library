# .createPlotScale

Translate scale into one of the values available in the enum `Scaling`.

## Usage

``` r
.createPlotScale(scale)
```

## Arguments

- scale:

  character defining the name of the scale

## Value

A value available in enum `Scaling`
