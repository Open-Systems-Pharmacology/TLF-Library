# loadThemeFromJson

Load theme object from json file. A template of a json theme is
available at system.file(package= "tlf",
"theme-maker","theme-template.json")

## Usage

``` r
loadThemeFromJson(jsonFile)
```

## Arguments

- jsonFile:

  path of json file

## Value

A `Theme` object
