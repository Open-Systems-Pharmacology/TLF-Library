# useTemplateTheme

Set default theme to be used as the current default of the tlf
environment

## Usage

``` r
useTemplateTheme()
```

## Examples

``` r
useTemplateTheme()
addScatter(x = seq(1, 10), y = rnorm(10))
```
