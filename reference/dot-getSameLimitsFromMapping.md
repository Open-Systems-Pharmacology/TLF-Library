# .getSameLimitsFromMapping

Get same axes limits from mapped data

## Usage

``` r
.getSameLimitsFromMapping(data, dataMapping)
```

## Arguments

- data:

  A data.frame with labels mapped to properties and obtained from a
  `DataMapping` object

- dataMapping:

  A `ObsVsPredDataMapping` object
