# useMinimalTheme

Set default minimal theme to be used as the current default of the tlf
environment

## Usage

``` r
useMinimalTheme()
```

## Examples

``` r
useMinimalTheme()
addScatter(x = seq(1, 10), y = rnorm(10))
```
