# setDefaultLogTicks

Set default values for log ticks

## Usage

``` r
setDefaultLogTicks(ticks)
```

## Arguments

- ticks:

  numeric values where ticks are placed. Ensure that the values are
  positive (they are meant for log scale)
