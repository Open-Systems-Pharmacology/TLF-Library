# .updateSameAxes

Updates plot configuration axes to get same limits

## Usage

``` r
.updateSameAxes(plotObject, data, dataMapping)
```

## Arguments

- plotObject:

  A `ggplot` object

- data:

  A data.frame

- dataMapping:

  A `DataMapping` object

## Value

A `ggplot` object
