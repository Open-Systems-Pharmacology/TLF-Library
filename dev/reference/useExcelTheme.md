# useExcelTheme

Set default Excel theme to be used as the current default of the tlf
environment

## Usage

``` r
useExcelTheme()
```

## Examples

``` r
useExcelTheme()
addScatter(x = seq(1, 10), y = rnorm(10))
```
