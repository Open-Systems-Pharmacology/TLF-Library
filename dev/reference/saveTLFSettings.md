# saveTLFSettings Save the current TLF global settings in a `.RData` file

saveTLFSettings Save the current TLF global settings in a `.RData` file

## Usage

``` r
saveTLFSettings(file)
```

## Arguments

- file:

  `.RData` file containing the settings
