# getTLFSettings

Get Names of the default/global settings stored in tlfEnv. Can be used
with `getTLFSettings()`

## Usage

``` r
getTLFSettings(settingName)
```

## Arguments

- settingName:

  setting name as defined in enum `tlfSettingsNames`
