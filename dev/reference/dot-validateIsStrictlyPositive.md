# Validate if values are strictly positive

Validate if values are strictly positive

## Usage

``` r
.validateIsStrictlyPositive(values, nullAllowed = FALSE)
```

## Arguments

- values:

  numeric values to be validated

- nullAllowed:

  logical allowing `NULL` as input
