# setDefaultMaxCharacterWidth

Set the maximum number of characters per row for axis ticks labels and
legend. Long strings will be wraped on spaces or non-word characters.

## Usage

``` r
setDefaultMaxCharacterWidth(maxCharacterWidth)
```

## Arguments

- maxCharacterWidth:

  the maximum number of characters per row
