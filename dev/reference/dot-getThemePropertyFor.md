# .getThemePropertyFor

Clone a theme plot configuration property so the property is not
affected in current theme

## Usage

``` r
.getThemePropertyFor(plotConfiguration, propertyName = NULL)
```

## Arguments

- plotConfiguration:

  `PlotConfigurations` object

- propertyName:

  Name of property. One `lines`, `points`, `ribbons` or `errorbars`
