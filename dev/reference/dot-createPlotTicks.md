# .createPlotTicks

Translate ticks and ticklabels into a value directly usable by `ggplot2`
to give more flexibility in the next functions

## Usage

``` r
.createPlotTicks(ticks)
```

## Arguments

- ticks:

  character, numeric or function defining the ticks

## Value

name of the `ggplot2` scale
