# useHighChartTheme

Set default HighChart theme to be used as the current default of the tlf
environment

## Usage

``` r
useHighChartTheme()
```

## Examples

``` r
useHighChartTheme()
addScatter(x = seq(1, 10), y = rnorm(10))
```
