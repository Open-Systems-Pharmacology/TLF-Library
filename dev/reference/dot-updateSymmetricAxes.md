# .updateSymmetricAxes

Updates plot configuration axes to get symmetric limits

## Usage

``` r
.updateSymmetricAxes(plotObject, data, dataMapping)
```

## Arguments

- plotObject:

  A `ggplot` object

- data:

  A data.frame

- dataMapping:

  A `DataMapping` object

## Value

A `ggplot` object
