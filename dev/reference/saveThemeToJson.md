# saveThemeToJson

Save theme object to a json file.

## Usage

``` r
saveThemeToJson(jsonFile, theme = NULL)
```

## Arguments

- jsonFile:

  path of json file

- theme:

  `Theme` object path of json file
