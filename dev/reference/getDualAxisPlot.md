# getDualAxisPlot

Check if dual Y Axis is needed

## Usage

``` r
getDualAxisPlot(leftPlotObject, rightPlotObject)
```

## Arguments

- leftPlotObject:

  A `ggplot` object with left y-axis

- rightPlotObject:

  A `ggplot` object with right y-axis

## Value

A `ggplot` object with dual y-axis
