# .setPlotObject

Set a `ggplot` object associated with its `plotConfiguration`

## Usage

``` r
.setPlotObject(plotObject, plotConfiguration = NULL)
```

## Arguments

- plotObject:

  A `ggplot` object

- plotConfiguration:

  A `PlotConfiguration` object

## Value

A `ggplot` object
